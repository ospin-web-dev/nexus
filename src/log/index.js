const device = require('./device')
const list = require('./list')
const listSubjects = require('./listSubjects')

/**
 * @namespace nexus.log
 */

module.exports = {
  device,
  list,
  listSubjects,
}
