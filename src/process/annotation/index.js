const create = require('./create')
const remove = require('./remove')
const update = require('./update')

/**
 * @namespace nexus.process.annotation
 */

module.exports = {
  create,
  remove,
  update,
}
