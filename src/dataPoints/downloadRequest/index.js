const createForProcess = require('./createForProcess')
const createForReporter = require('./createForReporter')
const list = require('./list')

/**
 * @namespace nexus.dataPoints.downloadRequest
 */

module.exports = {
  createForProcess,
  createForReporter,
  list,
}
