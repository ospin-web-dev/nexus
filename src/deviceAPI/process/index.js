const functionality = require('./functionality')
const stream = require('./stream')
const get = require('./get')

/**
 * @namespace nexus.deviceAPI.process
 */

module.exports = {
  functionality,
  stream,
  get,
}
